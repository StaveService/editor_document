import FixedSidebarLayout from "../../layouts/FixedSidebar"
import Pause from "../../components/ui/Pause"
import Stop from "../../components/ui/Stop"
import Volume from "../../components/ui/Volume"
import CountIn from "../../components/ui/CountIn"
import Loop from "../../components/ui/Loop"
import Metronome from "../../components/ui/Metronome"
import Layout from "../../components/ui/Layout"
import Zoom from "../../components/ui/Zoom"
export default FixedSidebarLayout

# UI Reference

## Header

- <Stop /> 停止

- <Pause /> 一時停止
- <Volume /> ドラッグ: 音量調節 クリック:ミュートまたはミュート解除
- <CountIn /> 再生直前に1小説待つ クリック: ON/OFF
- <Loop /> ループ クリック: ON/OFF
- <Metronome /> メトロノーム クリック: ON/OFF
- <Layout /> レイアウト変更 Page: 縦スクロール Horizontal: 横スクロール
- <Zoom /> 倍率変更
